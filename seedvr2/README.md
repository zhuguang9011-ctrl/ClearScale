# ClearScale SeedVR2 独立测试包

这是给 RTX 5060 Ti 8GB 准备的单图验证包，不需要 ComfyUI。它用于判断 SeedVR2 是否比 Finegrain 更适合“AI 商品图高清化、降低塑胶感并尽量保留结构”，目前不是正式版。

1. 完整解压到例如 `D:\ClearScale-SeedVR2`。
2. 双击 **Start.cmd**。首次安装并下载模型会很大，建议预留至少 25GB，保持窗口开启；随后浏览器打开 ClearScale Material Studio。
3. 在左侧“原图文件（必填）”上传一次原图，软件会自动把它复制到下方目标区域编辑器。如果要使用任何参考图，请在目标区域编辑器里涂抹需要处理的产品表面，再上传所需参考；没有涂抹区域时软件会拒绝全图套用。
4. 固定使用 3B Q4 GGUF、32 层 BlockSwap、CPU 卸载、512px VAE 分块、LAB 颜色校正、零噪声注入。无需手动调参数。
5. 网页版结果保存在程序目录的 `outputs\job_日期_时间` 中，也可直接点击右侧下载；`QuickStart.cmd` 的输出仍保存在原图旁边。文件名含 `_ClearScale_Fidelity_`，并生成验证报告。

启动器会先把图片转换到临时英文路径，规避 Windows/OpenCV 对中文安装目录和中文文件名的兼容问题，完成后再复制回原图目录。完整模型输出写入 `inference.log`；成功或失败后窗口会保留提示，不会无提示关闭。

原图使用独立上传框作为权威输入，目标区域编辑器只负责涂抹蒙版，避免 Chromium/Gradio 把可见图片存入不同图层字段而误报“未上传原图”。同时仍兼容旧的 `background` 与 `composite` 提交格式。

正式输出采用保真融合：原图的 Lanczos 高清版本作为主体，保留原始颜色、透明度、结构和已有纹理；SeedVR2 仅在边缘方向一致、局部细节能量没有下降的位置，以最高 28% 强度贡献亮度细节。白色线材等模型抹平的区域会自动退回原图。目标短边优先使用精确 2×，最高 2560px，长边最高 4096px。

选图后会显示操作选项：`Fidelity 15%` 最大程度保真，`Balanced 28%` 为默认建议，`Detail 40%` 提供更明显增强，`Raw SeedVR2` 保存模型原始结果用于比较；输出倍率可选 1.5× 或 2×。勾选 `Also save raw SeedVR2 result` 可以同时保存保真结果和模型原始结果。

网页界面把参考拆成四个独立、可选通道：`颜色参考`只迁移色相/饱和倾向并保留目标明暗；`材质参考`提供颗粒、纤维、光泽或哑光；`排列参考`提供线距、织法和方向性结构；`姿态/摆放参考`以实验性生成阶段控制整个产品的朝向、透视、位置和构图。任何通道都可留空，留空就完全跳过。前三个通道是确定性后处理；姿态通道仅在上传参考时才安装并下载 Stable Diffusion 1.5、IP-Adapter Plus 与 ControlNet Canny 模型，然后先重建姿态，再进入 SeedVR2 和其他参考通道。姿态阶段可能改变文字或细小结构，因此建议先用无文字产品图验证。

姿态阶段的安装、下载和推理记录单独保存在 `pose_reference.log`；SeedVR2 记录仍保存在 `inference.log`。

前三个参考图必须裁剪到纯参考区域，排除标签、文字、包装和背景；在原图上涂满目标表面，面积至少为全图 1%。使用姿态参考时，需要在原图中涂满要保持身份的产品，并在姿态参考图中涂满目标产品轮廓。成功结果文件名会列出实际使用的确定性通道，例如 `_ClearScale_MultiReference_color-material-arrangement_`。需要原来的弹窗快速测试时，可双击 `QuickStart.cmd`。

保真融合采用 512px 低内存分块，适合 8GB 显卡机器处理多百万像素结果。如果 SeedVR2 已成功而融合或参考材质后处理意外失败，程序会保留并返回模型原始 PNG，不再因后处理错误丢失整次结果；具体异常会追加到 `inference.log`。

测试时只判断五件事：孔位/开窗有没有改变、轮廓有没有白边、颜色是否偏移、表面是否仍有涂抹感、细节是否比普通放大更可信。任何一项明显失败，就不进入正式版。

代码来源固定为 [numz/ComfyUI-SeedVR2_VideoUpscaler](https://github.com/numz/ComfyUI-SeedVR2_VideoUpscaler) 提交 `4490bd1f482e026674543386bb2a4d176da245b9`，其代码使用 Apache-2.0。模型会从上游配置的 Hugging Face 仓库自动下载，模型权重许可需以对应模型页面为准。

限制：8GB 预设依赖 CPU 卸载，速度可能较慢；首次下载时间取决于网络。验证报告能证明文件、倍率、颜色与边缘指标，不能单独证明材质审美合格。

## 手动线材重建（试验）
此前排列参考只是纹理叠加，不能整理线条。新增的“线材重建”会在涂抹区域内平滑旧线纹并生成规则曲线。先只涂一个连续线材区域，调整线距（原图像素）、方向、弯曲程度与明暗强度，点击预览并保存。它不使用参考图，不会自动推断三维透视，也不保证透明材质的物理反射正确。蒙版外像素和透明度不变。不同开窗方向可能不同，应分别处理。结果为原图尺寸，满意后下载并重新上传进行高清放大；未满意时不要放大。无需下载额外模型。
