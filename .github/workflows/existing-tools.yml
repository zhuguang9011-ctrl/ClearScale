name: Existing tools starter package
on:
  push:
    branches: [development/surface-cleanup]
    paths: ['existing-tools/**', '.github/workflows/existing-tools.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python existing-tools/build_package.py
      - uses: actions/upload-artifact@v4
        with:
          name: Product-Editing-Starter
          path: existing-tools/dist/Product-Editing-Starter/
          if-no-files-found: error
          retention-days: 30
